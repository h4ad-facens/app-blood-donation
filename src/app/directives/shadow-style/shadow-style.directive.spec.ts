import { ShadowStyleDirective } from './shadow-style.directive';

describe('ShadowStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowStyleDirective();
    expect(directive).toBeTruthy();
  });
});
