import { DataScrollDirective } from './data-scroll.directive';

describe('DataScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DataScrollDirective();
    expect(directive).toBeTruthy();
  });
});
